# 异地开发与环境配置指南

这份文档将指导你如何在两台电脑之间进行协作开发（异地写代码），包括代码同步、数据库配置和开发环境搭建。

---

## 第一步：同步代码到 GitHub（在 A 电脑操作）

假设你在当前电脑（A 电脑）已经完成了部分开发。

1.  **打开 VS Code** 的源代码管理面板（左侧第三个图标）。
2.  **暂存更改**：点击 "更改" 旁边的 `+` 号，或者在终端运行 `git add .`。
3.  **提交更改**：在输入框填写备注（如 "完成POI功能"），点击 "提交" 或运行 `git commit -m "完成POI功能"`。
4.  **推送代码**：点击 "同步更改" 按钮，或在终端运行 `git push`。

---

## 第二步：新电脑环境准备（在 B 电脑操作）

在前往另一台电脑写代码前，请确保安装了以下基础软件：

1.  **VS Code**: 前端编辑器。
2.  **PyCharm** (可选，或继续用 VS Code): 后端编辑器。
3.  **Git**: 版本控制工具。
4.  **PostgreSQL & pgAdmin 4**: 数据库服务与管理工具。
5.  **Node.js**: 前端运行环境 (建议版本 v18+)。
6.  **Python**: 后端运行环境 (建议版本 3.10+)。

---

## 第三步：下载代码（在 B 电脑操作）

1.  在 B 电脑上创建一个工作目录（例如 `D:\Projects`）。
2.  打开该目录，鼠标右键点击 "Git Bash Here" 或打开终端。
3.  **克隆代码**：
    ```bash
    git clone https://github.com/你的用户名/你的仓库名.git
    ```
    *（如果已经克隆过，只需要进入目录运行 `git pull` 拉取最新代码）*

---

## 第四步：配置数据库（在 B 电脑操作）

后端代码运行依赖本地数据库，你需要在那边重新建立一个。

1.  **启动 pgAdmin 4**。
2.  连接到本地服务器（通常是 localhost:5432）。
3.  **创建数据库**：
    *   右键 `Databases` -> `Create` -> `Database`。
    *   Name 输入: `ota_dev` (这是项目默认配置的数据库名，不要改)。
    *   点击 Save。
4.  **验证用户**：
    *   确保你的 PostgreSQL 有一个用户名为 `postgres`，密码为 `postgres` 的超管账户。
    *   *如果你设置了不同的密码*，需要修改代码里的配置文件（见下文）。

---

## 第五步：配置后端环境 (PyCharm / Python)

1.  **打开项目**：使用 PyCharm 打开克隆下来的项目根目录（或 `backend` 目录）。
2.  **配置虚拟环境**：
    *   PyCharm 会提示 "No Python interpreter configured"。
    *   点击 `Add Interpreter` -> `Add Local Interpreter` -> `Virtualenv Environment`。
    *   选择 `New`，位置默认即可，Base interpreter 选择你安装的 Python。
3.  **安装依赖**：
    *   打开 PyCharm 的终端（Terminal）。
    *   进入后端目录（如果不在的话）：`cd backend`。
    *   运行安装命令：
        ```bash
        pip install -r requirements.txt
        ```
4.  **配置环境变量**：
    *   在 `backend` 目录下创建一个名为 `.env` 的文件。
    *   复制以下内容（根据 B 电脑实际数据库密码修改）：
        ```ini
        DB_HOST=localhost
        DB_PORT=5432
        DB_USER=postgres
        DB_PASSWORD=YOUR_PASSWORD_HERE
        DB_NAME=ota_dev
        JWT_SECRET=your_secret_key_here
        ```
        *(如果是默认密码 postgres，可以跳过此步，项目有默认配置)*。

---

## 第六步：数据库迁移（结构同步）

现在你有了一个空的数据库，需要把表结构创建出来。

1.  在 `backend` 目录下运行终端。
2.  **运行迁移命令**：
    ```bash
    alembic upgrade head
    ```
    *成功后，你的 `ota_dev` 数据库里应该会出现 product, sku 等表格。*

### (可选) 数据同步
如果 A 电脑里有重要的测试数据需要带过去：
1.  **A 电脑导出**：在 pgAdmin 右键数据库 -> Backup -> 格式选择 Custom 或 Tar。
2.  **B 电脑导入**：在 pgAdmin 右键新数据库 -> Restore -> 选择刚才的文件。
*通常开发阶段，直接用迁移命令创建空表，然后重新造一点测试数据即可。*

---

## 第七步：配置前端环境 (VS Code)

1.  用 VS Code 打开项目的 `frontend` 目录。
2.  打开终端，安装依赖：
    ```bash
    npm install
    # 或者如果用了 yarn
    yarn
    ```
3.  启动前端：
    ```bash
    npm run dev
    ```

---

## 第八步：异地开发与同步循环

现在环境都配置好了，你可以开始写代码了。

1.  **开始工作前**：
    *   `git pull` (确保拿到 A 电脑最新的改动)。
    *   启动后端：PyCharm 中运行 `main.py` 或终端执行 `uvicorn app.main:app --reload`。
    *   启动前端：VS Code 中运行 `npm run dev`。
2.  **完成工作后**（要离开 B 电脑了）：
    *   `git add .`
    *   `git commit -m "在B电脑完成了XX功能"`
    *   `git push`
3.  **回到 A 电脑**：
    *   `git pull` (拉取在 B 电脑写的代码)。

---

## 速查表

| 操作 | 命令/步骤 |
| :--- | :--- |
| **拉取最新** | `git pull` |
| **提交代码** | `git add .` -> `git commit` -> `git push` |
| **启动后端** | `cd backend` -> `uvicorn app.main:app --reload` |
| **启动前端** | `cd frontend` -> `npm run dev` |
| **数据库变更**| `alembic revision --autogenerate -m "描述"` -> `alembic upgrade head` |
