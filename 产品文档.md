# 产品文档（2026-01）

面向 OTA/旅游场景的红猪数据库，覆盖“供应商 → 资源 → 产品 → SKU/渠道 → 价格/库存 → 订单 → 审批/审计 → 报表”的全链路。当前代码已提供后端 API 与 React 管理后台。

## 1. 模块进度
- **M1 供应商**：供应商 CRUD，联系方式/结算信息/资质等扩展字段；供应商-资源绑定，支持结算价调价（写入历史与审批）；供应商资源日历库存批量设置。
- **M2 资源中心**：POI、资源 CRUD（全局唯一名校验、审计日志）；资源删除会校验产品引用；支持按 POI/类型/关键字分页筛选。
- **M3 产品管理**：产品分类 CRUD；产品创建/编辑（资源行、建议价/成本、结构哈希去重、可售渠道 `allowed_channels`）；产品结构快照；按资源库存计算产品可售量与预览。
- **M4 SKU & 渠道**：SKU CRUD（售卖/出行时间窗、继承产品 POI），非管理员修改/上下架会生成审批；渠道 CRUD（树形、佣金率、状态）及审批；SKU-渠道绑定唯一校验。
- **M5 定价**：价格日历段创建自动处理重叠（覆盖/切尾/拆段），非管理员默认为 `pending` 待审；价格审批/决策接口；价格历史；定价总览汇总 SKU×渠道当前价区间。
- **M6 库存**：SKU 库存批量初始化与单日手动调整，记录 `InventoryLog`；按日查看 SKU 库存；供应商资源库存日历（含日结算价、工作日筛选）；SKU/渠道库存=产品库存×渠道占比。
- **M7 订单**：手工下单（校验重复订单号+渠道），按出行日动态取资源结算价计算成本；冻结库存，核销/退款时消费或释放库存并记状态历史。
- **M8 审批与审计**：审批列表/决策；价格审批在价格决策接口中处理；全量审计日志，可按表/操作人筛选；超管可批量清理审批、审计记录。
- **M9 报表**：`/reports/summary` 输出 GMV/利润/订单趋势，渠道/产品/SKU TOP 榜；前端包含销售、利润看板。

## 2. 角色与权限概要
- 角色：`super_admin`（全权限+用户管理+批量清理）、`admin`（全业务直写）、`product`（资源/产品全权，敏感操作需审批）、`operator`（订单/商品运营，敏感操作需审批）、`csr`（客服只读或受限操作）。  
- 审批触发（非 admin/super_admin）：SKU 状态或已上架信息变更、渠道创建/删除/敏感字段变更、价格创建或覆盖、部分结算价调整等。  
- 详情规则与菜单可见性参见 `permission_system.md`，前端菜单按角色动态收敛；部分按钮在低权限时只读或隐藏。

## 3. 核心数据模型（摘自后端模型）
- **Poi**：`poi_name`、`poi_type`、`city`、`address`、`tags`、`status`。
- **Resource**：`resource_name`、`resource_type`、`poi_id`、`attrs`(JSONB)、`status`；全局唯一名。
- **Supplier & SupplierResource**：供应商基础信息；供应商-资源绑定含 `settlement_price`、`currency`、`priority`、`rule`、`supply_status`。
- **SupplierResourcePriceHistory / ResourceInventory**：结算价变更历史；供应商资源日历库存（`inventory_date`、`settlement_price`、`total/frozen/sold`、`status`）。
- **ProductCategory / Product**：分类；产品含 `structure_hash` 去重、`allowed_channels`(channel_id+stock_ratio) 限定可售渠道与库存占比、`suggested_price`、`base_cost`、`poi_id`。
- **ProductResource / ProductStructureSnapshot**：产品-资源行（可指定 supplier、数量、必选）；结构快照存档。
- **Channel**：层级渠道，`channel_type`、`commission_rate`、`status`。
- **Sku / SkuChannel**：SKU（售卖/出行时间窗、`poi_id` 继承产品）；SKU-渠道绑定、渠道 SKU 编码。
- **Price / PriceHistory**：SKU×渠道日历价（`sale_price`、`cost_price`、`start_at/end_at`、`status` pending/active/expired/rejected）；重叠处理与审批；变更历史。
- **Inventory / InventoryLog**：SKU 日库存（`total/frozen/sold`），调节/冻结/核销/解冻日志。
- **Order / OrderStatusHistory**：订单号+渠道唯一；`sale_amount`、`cost_amount`、`profit_amount`、状态 `paid/verified/refunded` 等；状态历史。
- **Approval / AuditLog**：审批对象/动作/前后数据/申请人/审批人/状态；审计记录表名、记录 ID、diff、操作者、来源。
- **User**：`username`、`password_hash`、`role`。

## 4. 关键流程
- **登录鉴权**：`POST /api/auth/login` 返回 Bearer Token；前端存储在 localStorage，并在请求头传递 `Authorization` 与 `X-Role`。支持 `X-User/X-Role` 直通用于内网透传。
- **资源建模**：创建 POI → 资源（唯一名+审计）→ 供应商 → 供应商-资源绑定（结算价、优先级）；结算价调整会写历史并生成审批；供应商资源可批量设置日历库存/结算价。
- **产品定义**：维护产品分类 → 创建产品（资源行不可重复；自动推断 POI；可售渠道/库存占比）→ 可生成结构快照；`/products/{id}/inventory` 基于资源库存计算可售量，`/products/inventory/preview` 支持假设组合预估。
- **SKU / 渠道**：SKU 创建继承产品 POI，非管理员编辑/上下架会写 `Approval`；渠道新增/删除/佣金率变更需审批；SKU-渠道绑定校验同渠道同名 SKU 唯一。
- **定价**：价格创建会查找重叠并按规则截断/覆盖，非管理员标记为 `pending`；价格决策接口 `/prices/{id}/decision` 驳回/生效并处理其他重叠价格与历史；价格历史 `/price-history`；渠道库存上限通过产品 `allowed_channels` 占比计算，定价中心日历（前端当前只读查看）。
- **库存**：SKU 库存初始化 `/inventory/init`（支持 weekday 过滤，返回统计审计）；单日手工调节 `/inventory/adjust` 写日志；按日查询 `/inventory/day`；供应商资源库存批量接口 `/supplier-resources/inventory/batch` 支持日结算价；产品/渠道库存计算接口用于价格/库存日历。
- **订单**：创建订单 `/orders` 校验重复订单号+渠道；按出行日查询供应商资源日历价或默认结算价计算成本；下单冻结库存，核销/退款 `/orders/{id}/decision` 消费或释放库存并记录状态历史与审计。
- **审批与审计**：`/approvals` 列表；非价格类审批的通过/驳回在审批接口执行，价格审批在价格决策接口执行；审计日志 `/audit-log` 可筛选并支持超管批量删除。
- **报表**：`/reports/summary` 按日/周/月输出 GMV/利润/订单数、核销/退款数，以及渠道/产品/SKU TOP5；前端销售/利润页读取该接口。

## 5. 前端界面与交互
- **运营看板**：Dashboard（销售/利润/任务占位）。
- **资源中心**：POI 列表、资源列表；供应商列表，支持查看绑定资源、结算价历史、供应商资源库存。
- **产品管理**：产品列表、分类管理、产品编辑器（资源选择、可售渠道占比、快照）。
- **SKU 与渠道**：SKU 列表；渠道管理（树形、佣金率、状态、审批）；SKU-渠道绑定列表。
- **定价中心**：定价总览表（SKU×渠道价区间、筛选）；日历弹窗只读查看价格+库存上限；价格历史抽屉。
- **库存**：按日查看 SKU 库存，显示渠道、已冻结/已核销/可用量。
- **订单**：订单列表；新建订单弹窗（选择渠道/SKU/产品、出行日、数量、售价）。
- **审批中心**：待审批列表，管理员可审批；超管可批量删除审批记录。
- **操作日志**：审计日志列表。
- **报表**：销售报表、利润报表页。
- **用户管理**：仅超管可见，管理角色分配。
- 数据加载：登录后 `DataProvider` 一次性拉取各列表（`?page=1&page_size=1000`），菜单/按钮按 `user.role` 收敛。

## 6. 已知限制与下一步
- 定价中心前端当前默认只读（写入需调用价格/库存接口，可按需开放）；未接入外部渠道 API。
- 未提供自动化测试与 CI，修改模型需补充 Alembic 迁移；审计/审批批量删除需谨慎使用，仅限超管。
- 订单导入、库存告警、更多 BI 指标可按业务需要后续扩展。
