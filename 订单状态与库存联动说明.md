# 订单状态与库存联动说明（SKU 与子资源）

本说明基于当前代码行为（`backend/app/api/endpoints/orders.py`），描述**订单状态变更**对**SKU 日库存**与**子资源日历库存**的影响。  
当前实现：**出行日期与状态字段按“子资源维度”存储**（`OrderResource`），订单层仅保留汇总展示用日期/状态。
统一口径：POI 显示为“资源”，Resource 显示为“子资源”。

---

## 1. 先给结论（规则摘要）

| 场景 | 对 SKU 库存 | 对 子资源库存 |
|---|---|---|
| **下单创建** | 冻结 `frozen_qty += 下单数量` | 冻结对应供应商子资源 `frozen_qty += 资源用量` |
| **核销**（verify） | `frozen_qty -= 数量`；`sold_qty += 数量` | 同步**消耗**：`frozen_qty -= 资源用量`；`sold_qty += 资源用量` |
| **未核销/未预约退款** | `frozen_qty -= 数量`（释放） | 同步**释放**冻结：`frozen_qty -= 资源用量` |
| **已核销/已预约退款** | `sold_qty -= 数量`（回补） | 同步**回补**已售：`sold_qty -= 资源用量` |

> 说明：  
> - “数量”指订单维度的退款/核销数量；  
> - “资源用量”= 订单数量 × 该子资源行的 `quantity`（产品-子资源行配置的数量）。

---

## 2. 订单创建时的库存冻结

**触发**：`POST /orders`  
**行为**：
1. 先算出每条产品-子资源行的**实际供应商**与结算价（可手选或自动选择）。
2. 写入 `OrderResource`（记录子资源 + 供应商 + 结算价 + 用量）。
3. 冻结库存：
   - **SKU 日库存**：按子资源出行日期去重后逐日冻结  
     `Inventory.frozen_qty += 订单数量`
   - **子资源日历库存**：对每条 `OrderResource` 记录  
     `ResourceInventory.frozen_qty += 资源用量`

> 若库存不足（SKU 或子资源），下单会失败并回滚。

---

## 3. 核销（verify）的库存变化

**触发**：`POST /orders/{id}/decision`，`action = verify`  
**行为**：
1. **SKU 库存**：
   - 冻结减少：`frozen_qty -= 核销数量`
   - 已售增加：`sold_qty += 核销数量`
2. **子资源库存**（按订单资源明细逐条执行）：
   - 冻结减少：`frozen_qty -= 资源用量`
   - 已售增加：`sold_qty += 资源用量`

---

## 4. 退款（未核销 / 未预约）

### 4.1 未核销退款（refund_unverified）
**触发**：`action = refund_unverified`  
**逻辑约束**：
- 退款数量 ≤ 订单数量 − 已核销数量  
  （可理解为“还没核销的部分才能做未核销退款”）

**库存变化**：
- **SKU**：释放冻结 `frozen_qty -= 退款数量`
- **子资源**：释放冻结 `frozen_qty -= 资源用量`

### 4.2 未预约退款（refund_unreserved）
**触发**：`action = refund_unreserved`  
**逻辑约束**：
- 退款数量 ≤ 订单数量 − 已预约数量

**库存变化**：
- **SKU**：释放冻结 `frozen_qty -= 退款数量`
- **子资源**：释放冻结 `frozen_qty -= 资源用量`

---

## 5. 退款（已核销 / 已预约）

### 5.1 已核销退款（refund_verified）
**触发**：`action = refund_verified`  
**逻辑约束**：
- 退款数量 ≤ 已核销数量

**库存变化**：
- **SKU**：回补已售 `sold_qty -= 退款数量`
- **子资源**：回补已售 `sold_qty -= 资源用量`

### 5.2 已预约退款（refund_reserved）
**触发**：`action = refund_reserved`  
**逻辑约束**：
- 退款数量 ≤ 已预约数量

**库存变化**：
- **SKU**：回补已售 `sold_qty -= 退款数量`
- **子资源**：回补已售 `sold_qty -= 资源用量`

---

## 6. 其他状态（出票/预约/完成/纠纷）

这些状态字段目前**不会直接影响库存**，只会写入订单状态字段与状态历史。  
库存变更只发生在：
- 下单冻结
- 核销（verify）
- 退款（未核销/未预约/已核销/已预约）

---

## 7. 一句话流程图（文本）

```
下单 → 冻结 SKU/子资源库存
核销 → SKU/子资源：冻结转已售
未核销/未预约退款 → SKU/子资源：释放冻结
已核销/已预约退款 → SKU/子资源：回补已售
```
