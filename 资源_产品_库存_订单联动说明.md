# 资源/子资源/供应商/产品/SPU/SKU/库存/订单联动说明

本说明基于当前代码实现，统一口径：  
**POI 在前端显示为「资源」**，**Resource 在前端显示为「子资源」**。

---

## 1. 实体含义与包含关系（文字版）

- **资源(POI)**：顶层业务载体（景区/酒店/交通等的“主体”）。  
- **子资源(Resource)**：资源下的具体可供给项（房型/门票/车次等），全局唯一名。  
  - 子资源可绑定多个供应商（`SupplierResource`）。
- **供应商(Supplier)**：提供子资源的供给方。  
  - **供应商-子资源绑定(SupplierResource)**：记录结算价、供给状态、优先级等。
- **产品(Product)**：可售商品组合，由多条**产品-子资源行**组成。  
  - **产品-子资源行(ProductResource)**：指定子资源、数量、是否必选、供应商模式（锁定/自动）。
  - 产品包含**可售渠道占比** `allowed_channels`（渠道 + 库存占比）。
- **SPU**：SKU 的父级分组。
- **SKU**：可销售单元，绑定一个产品 + 一个 SPU，并继承资源(POI)。  
  - SKU 与渠道通过 `SkuChannel` 绑定（必须绑定才可下单）。
- **库存(Inventory)**：SKU 按日库存，**由产品子资源库存×渠道占比自动计算**。
- **订单(Order)**：订单号 + 渠道唯一；创建时选择**渠道 → SPU → SKU**；支付时间必填。  
  - **订单子资源明细(OrderResource)**：记录订单实际使用的子资源 + 供应商 + 结算价。  
  - **订单状态历史(OrderStatusHistory)**：记录核销/退款等状态变更。

---

## 2. 实体关系图（Mermaid）

```mermaid
graph TD
    POI[资源(POI)]
    RES[子资源(Resource)]
    SUP[供应商(Supplier)]
    SR[供应商-子资源绑定<br/>SupplierResource]
    RI[子资源日历库存<br/>ResourceInventory]

    PROD[产品(Product)]
    PR[产品-子资源行<br/>ProductResource]
    AC[可售渠道占比<br/>allowed_channels]

    SPU[SPU]
    SKU[SKU]
    CH[渠道(Channel)]
    SCH[SKU-渠道绑定<br/>SkuChannel]

    INV[SKU日库存<br/>Inventory]
    ORD[订单(Order)]
    OR[订单子资源明细<br/>OrderResource]
    OSH[订单状态历史<br/>OrderStatusHistory]

    POI --> RES
    RES --> SR
    SUP --> SR
    SR --> RI

    PROD --> PR
    RES --> PR
    PROD --> AC

    SPU --> SKU
    PROD --> SKU
    SKU --> SCH
    CH --> SCH

    PROD --> INV
    AC --> INV
    RI --> INV

    SKU --> ORD
    CH --> ORD
    PROD --> ORD
    ORD --> OR
    SR --> OR
    ORD --> OSH
```

---

## 3. 资源库存 → 产品库存（木桶算法）

**目标**：算出某天 `产品可售量`。  
**核心**：取所有**必选子资源**的**可用库存**，按行数量折算后取最小值（木桶算法）。

1. 对产品的每条必选子资源行 `ProductResource`：
   - 若供应商模式为 **locked**：只统计指定供应商集合的库存总和。  
   - 若为 **auto**：统计该子资源所有可用供应商库存总和。
2. 对每条子资源行计算：  
   `可售份数 = 子资源可用库存 / 该行数量（向下取整）`
3. 取所有必选子资源行的 **最小值** 作为该产品当天可售量。

> 若子资源日历库存未设置，默认可用为 0，产品当天可售量为 0。

---

## 4. 产品库存 → SKU 日库存（渠道占比）

**SKU 日总库存**由产品可售量 × 渠道占比得到：

```
SKU_总库存(某天) = floor(产品可售量(某天) × 渠道占比/100)
```

- 渠道占比来源：`Product.allowed_channels`。  
- 若该产品未配置该渠道占比 → **不可下单**。  
- 手动初始化/调整 SKU 库存已禁用，库存由系统自动计算写入/刷新。

---

## 5. 订单与库存的联动

### 5.1 订单创建（/orders）

1. 校验：
   - 订单号+渠道唯一
   - 渠道必须绑定 SKU
   - 产品已配置该渠道占比
   - 支付时间必填
2. 选择供应商（针对每条产品-子资源行）：
   - 若锁定供应商 → 仅在锁定范围内选
   - 若自动模式 → 选库存充足且结算价更优者
3. 创建订单并写入 `OrderResource`（记录子资源+供应商+结算价）。
4. **库存冻结**：
   - 冻结 **SKU 日库存**（`Inventory.frozen_qty`）
   - 冻结 **子资源日历库存**（`ResourceInventory.frozen_qty`）

### 5.2 核销/退款（/orders/{id}/decision）

- **核销**：减少 `frozen_qty`，增加 `sold_qty`  
  - SKU 库存与子资源库存同步扣减
- **退款**：按类型释放或回补（根据规则校验数量上限）
  - 已核销退款 → 回补 `sold_qty`
  - 未核销退款/未预约退款 → 释放 `frozen_qty`

---

## 6. 订单状态字段（独立字段存储）

每组状态包含：**是否(布尔) + 时间(必填) + 可选数量/金额**。  
前端列表只展示“是”的状态标签。

当前状态组：
- 出票/发码/发短信
- 核销
- 预约
- 退款（未核销 / 未预约 / 已核销 / 已预约）
- 完成
- 纠纷
- 支付（必填时间）

---

## 7. 关键约束（摘要）

- 订单号 + 渠道唯一。  
- SKU 必须绑定渠道（`SkuChannel` active）。  
- 产品必须配置对应渠道的库存占比。  
- 退款数量不能超过对应“已核销/已预约”数量或未处理数量上限。  
- 日期字段：出行日期为 `YYYY-MM-DD`，状态时间为 `YYYY-MM-DD HH:mm`。

---

## 8. 一句话总结联动路径

**子资源库存 → 产品可售量 → 渠道占比 → SKU 日库存 → 订单冻结/核销/退款 → 子资源与 SKU 库存同步变动**。

